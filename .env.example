# Required: your Ollama Cloud API key
OLLAMA_API_KEY=

# Optional: path to your project workspace (mounted into the opencode container)
WORKSPACE=./workspace

# Optional: override ports
UI_PORT=3000
OPENCODE_PORT=4000
